<UserControl x:Class="GestionApp.Views.CombosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:GestionApp.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:CombosViewModel}"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#F5F5F5">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Encabezado -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel>
                <TextBlock Text="Combos" 
                           FontSize="28" 
                           FontWeight="Bold" 
                           Foreground="#1A237E"/>
                <TextBlock Text="Crea y gestiona paquetes de productos" 
                           FontSize="14" 
                           Foreground="#666"/>
            </StackPanel>
            
            <Button Grid.Column="1" 
                    Content="➕ Crear Combo" 
                    Command="{Binding CrearComboCommand}"
                    Background="#7B1FA2" 
                    Foreground="White"
                    Padding="20,10"
                    FontSize="13"
                    BorderThickness="0"
                    Cursor="Hand"/>
        </Grid>
        
        <!-- Filtros -->
        <Border Grid.Row="1" 
                Style="{StaticResource CardStyle}" 
                Margin="0,0,0,15"
                Padding="15">
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding Filtro, UpdateSourceTrigger=PropertyChanged}" 
                         Width="250" 
                         Padding="10,8"
                         FontSize="13"/>
                
                <ComboBox ItemsSource="{Binding TiposCombo}"
                          SelectedItem="{Binding FiltroTipo}"
                          Margin="15,0,0,0"
                          Width="150"
                          Padding="10,8">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>
        </Border>
        
        <!-- Lista de combos -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0">
            <DataGrid ItemsSource="{Binding Combos}" 
                      SelectedItem="{Binding ComboSeleccionado}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      BorderThickness="0"
                      Background="Transparent">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nombre" 
                                        Binding="{Binding Nombre}" 
                                        Width="*"/>
                    <DataGridTextColumn Header="Tipo" 
                                        Binding="{Binding Tipo}" 
                                        Width="100"/>
                    <DataGridTextColumn Header="Productos" 
                                        Binding="{Binding Productos.Count}" 
                                        Width="80"/>
                    <DataGridTextColumn Header="Costo" 
                                        Binding="{Binding CostoTotal, StringFormat=C2}" 
                                        Width="100"/>
                    <DataGridTextColumn Header="Precio Venta" 
                                        Binding="{Binding PrecioVenta, StringFormat=C2}" 
                                        Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
